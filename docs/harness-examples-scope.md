# Operator Harness Examples - Approved Scope

Amit requested 2-3 deployed AgentCore Harness examples, copy/paste prompts, AWS CLI and GUI test steps, and actual testing of each use case.

This work belongs to `mytestlab123/lab1_agent`. The SecCop repository and its workloads remain read-only and unchanged. The earlier audit-correlation adoption proposal is not being implemented.

Planned examples:

1. Pasted finding explainer: no effective tools and no infrastructure mutation.
2. Live read-only lab checker: one exact lab SSM parameter read through a narrowly scoped Gateway/Lambda tool; no writes to the parameter.
3. Approval practice: a typed inline-function pause and explicit APPROVED/REJECTED resume, clearly labeled a simulation with no remediation tool.

Use personal lab Singapore only, reverify STS, retain useful low-cost resources, set explicit inference/session limits, and never publish credentials or raw private inventory. Publish test results distinguishing API/terminal evidence from any GUI steps not directly exercised. Do not represent a model's text or an automated test decision as an authenticated human approval.
